{
  "identifier": "default",
  "description": "Default capabilities for Gander",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "window:allow-set-badge-count"
  ]
}
